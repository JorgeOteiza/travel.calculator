export const GOOGLE_MAPS_LIBRARIES = ["places"];

// Centro predeterminado si no se obtiene la ubicación actual (Santiago, Chile)
export const DEFAULT_MAP_CENTER = {
  lat: -33.4489,
  lng: -70.6693,
};

export const MAP_OPTIONS = {
  disableDefaultUI: true,
  zoomControl: true,
};
